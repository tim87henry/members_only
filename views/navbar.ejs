<nav class="navbar navbar-expand-lg navbar-light mb-4" style="background-color: rgb(235, 235, 235);">
    <div class="container-fluid">
        <div class="navbar-header">
        <a class="navbar-brand" href="/">ClubHouse</a>
        </div>
        <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= (typeof title != 'undefined' && title==='ClubHouse')?'active':undefined %>"><a class="nav-link" href="/">Home</a></li>
        <% if(typeof currentUser != 'undefined' && currentUser) { %>
            <li class="nav-item <%= (typeof title != 'undefined' && title==='Create a post')?'active':undefined %>"><a class="nav-link" href='/message/create'>Add a message</a></li>
            <% if(!currentUser.is_member) { %>
                <li class="nav-item <%= (typeof title != 'undefined' && title==='Join Club')?'active':undefined %>"><a class="nav-link" href=<%= '/user/'+currentUser.id+"/join" %>>Become a member</a></li>
            <% } else if (!currentUser.is_admin) { %>
                <li class="nav-item <%= (typeof title != 'undefined' && title==='Upgrade yourself')?'active':undefined %>"><a class="nav-link"href=<%= '/user/'+currentUser.id+"/admin" %>>Become an admin</a></li>
            <% } %>
            <li class="nav-item"><a class="nav-link" href="/sign_out">Sign Out</a></li>
        <% } else { %>
            <li class="nav-item <%= (typeof title != 'undefined' && title==='Sign In')?'active':undefined %>"><a class="nav-link" href="/sign_in">Sign In</a></li>
            <li class="nav-item <%= (typeof title != 'undefined' && title==='Sign Up')?'active':undefined %>"><a class="nav-link" href="/user/sign_up">Sign Up</a></li>
        <% } %>
        </ul>
    </div>
</nav>